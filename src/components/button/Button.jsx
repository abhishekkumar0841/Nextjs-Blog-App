import React from "react";

const Button = ({ children }) => {
  return <button className=" bg-sky-400 text-gray-100 px-6 py-1 rounded-md font-bold w-fit">{children}</button>;
};

export default Button;
